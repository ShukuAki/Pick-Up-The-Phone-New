@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #FAF4EF;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow: hidden;
        }

        .splash-container {
            position: relative;
            width: 100%;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            opacity: 0;
            animation: fadeInOut 7s ease-in-out forwards;
        }

        .splash-image {
            max-width: 80%;
            max-height: 80vh;
            object-fit: contain;
        }

        @@keyframes fadeInOut {
            0% {
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                opacity: 0;
            }
        }
    </style>
</head>
<body>
    <div class="splash-container">
        <img src="/images/sunset.gif" alt="Welcome" class="splash-image">
    </div>

    <script>
        // Redirect after 7 seconds
        setTimeout(function() {
            window.location.href = '@Url.Action("Index", "Music")';
        }, 7000);
    </script>
</body>
</html> 